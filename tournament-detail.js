(function () {
  const key = 'wavehub.tournaments';
  const sessionKey = 'wavehub.session';
  const id = new URLSearchParams(location.search).get('id');
  const byId = (name) => document.getElementById(name);
  const read = () => { try { return JSON.parse(localStorage.getItem(key) || '[]'); } catch { return []; } };
  const covers = { 'PUBG Mobile': 'assets/pubg-photo.jpeg', 'Free Fire': 'assets/freefire-photo.jpeg', 'Standoff 2': 'assets/home-game-standoff2.png', 'Call of Duty': 'assets/cod-photo.jpeg', 'Mobile Legends': 'assets/home-game-mobile-legends.png' };
  const icons = { calendar: '<svg viewBox="0 0 24 24"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M16 3v4M8 3v4M3 10h18"/></svg>', globe: '<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3a14 14 0 0 1 0 18M12 3a14 14 0 0 0 0 18"/></svg>', users: '<svg viewBox="0 0 24 24"><path d="M16 20v-2a4 4 0 0 0-4-4H6a4 4 0 1 0 0-8 4 4 0 0 0 0 8"/></svg>', pin: '<svg viewBox="0 0 24 24"><path d="M20 10c0 6-8 11-8 11S4 16 4 10a8 8 0 1 1 16 0Z"/><circle cx="12" cy="10" r="2"/></svg>', layers: '<svg viewBox="0 0 24 24"><path d="m12 3 9 5-9 5-9-5 9-5ZM3 12l9 5 9-5M3 16l9 5 9-5"/></svg>', trophy: '<svg viewBox="0 0 24 24"><path d="M7 3h10v5a5 5 0 0 1-10 0V3Z"/><path d="M7 5H3v2a4 4 0 0 0 4 4m10-6h4v2a4 4 0 0 1-4 4M12 13v4m-4 4h8m-6-4h4"/></svg>', ticket: '<svg viewBox="0 0 24 24"><path d="M4 7h16v4a2 2 0 0 0 0 4v4H4v-4a2 2 0 0 0 0-4V7Z"/><path d="M14 8v2m0 2v2m0 2v2"/></svg>', info: '<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 11v5m0-8h.01"/></svg>', file: '<svg viewBox="0 0 24 24"><path d="M6 3h8l4 4v14H6V3Z"/><path d="M14 3v5h5M9 13h6M9 17h6"/></svg>', crosshair: '<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="6"/><path d="M12 2v4m0 12v4M2 12h4m12 0h4M12 9v6m-3-3h6"/></svg>', phone: '<svg viewBox="0 0 24 24"><rect x="7" y="2" width="10" height="20" rx="2"/><path d="M11 18h2"/></svg>', clock: '<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>', play: '<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="m10 8 6 4-6 4V8Z"/></svg>', arrow: '<svg viewBox="0 0 24 24"><path d="M5 12h14M13 6l6 6-6 6"/></svg>' };
  let filter = 'all';
  let tournament;
  const esc = (value) => String(value ?? '').replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;').replaceAll("'", '&#039;');
  const date = (value) => value ? new Date(`${value}T12:00:00`).toLocaleDateString('en-US', { day: 'numeric', month: 'short', year: 'numeric' }) : 'Date TBA';
  const kind = (match) => { const value = String(match.stage || 'group').toLowerCase(); return value.includes('group') ? 'group' : value.includes('quarter') || value.includes('semi') || value.includes('final') || value.includes('playoff') ? 'playoff' : value; };
  function row(match) { const state = String(match.status || 'completed').toLowerCase(); const scoreA = Number.isFinite(Number(match.scoreA)) ? match.scoreA : '-'; const scoreB = Number.isFinite(Number(match.scoreB)) ? match.scoreB : '-'; const result = state === 'live' ? 'Live' : state === 'scheduled' ? 'Scheduled' : match.result || 'Final'; return `<article class="match-row ${state}" data-stage="${kind(match)}"><div class="match-stage ${kind(match)}">${esc(match.stage || 'Group Stage')}</div><div class="match-teams"><strong>${esc(match.teamA || 'Team A')}</strong><span>vs</span><strong>${esc(match.teamB || 'Team B')}</strong></div><div class="match-info"><span>${icons.pin}${esc(match.map || 'Map TBA')}</span><span>${icons.layers}${esc(match.format || tournament.format || 'Format TBA')}</span><span>${icons.calendar}${date(match.date)}${match.time ? ` ${esc(match.time)}` : ''}</span></div><div class="match-result ${state}"><b>${esc(result)}</b><strong>${scoreA} - ${scoreB}</strong></div><a class="match-details" href="${esc(match.detailsUrl || '#matchHistoryTitle')}">View Details ${icons.arrow}</a></article>`; }
  function renderPrejoin() { const max = Number(tournament.maxPlayers) || 64; const players = Math.min(max, Number(tournament.players) || 0); const image = tournament.coverData || covers[tournament.game] || covers['PUBG Mobile']; const fee = tournament.entryFee || 'To be announced'; const field = (label, value, icon = '') => `<p${icon ? ' class="prejoin-fact"' : ''}>${icon}<span><small>${label}</small><strong>${esc(value || 'To be announced')}</strong></span></p>`; byId('tournamentDetailPage').innerHTML = `<section class="prejoin-page"><header class="prejoin-hero" style="background-image:linear-gradient(90deg,rgba(2,6,14,.1),rgba(2,6,14,.42)),url('${esc(image)}')"><div><span>WAVEHUBX</span><h1>${esc(tournament.name)}</h1><strong>${esc(tournament.tagline || 'COMPETE. IMPROVE. WIN.')}</strong></div></header><section class="prejoin-summary"><div>${icons.users}${field('Registration', String(tournament.status || 'upcoming').toUpperCase())}</div><div>${icons.calendar}${field('Tournament Date', date(tournament.startDate))}</div><div>${icons.users}${field('Players', `${players} / ${max}`)}</div><div>${icons.trophy}${field('Prize Pool', tournament.prize)}</div><div>${icons.ticket}${field('Entry Fee', fee)}</div></section><section class="prejoin-details"><nav><button class="active">${icons.info}<span>General</span></button><button>${icons.trophy}<span>Prize Pool</span></button><button>${icons.file}<span>Rules</span></button><button>${icons.users}<span>Teams</span></button></nav><div class="prejoin-grid"><div class="prejoin-facts">${field('Format', tournament.format, icons.users)}${field('Mode', tournament.mode, icons.crosshair)}${field('Region / Server', tournament.region, icons.globe)}${field('Platform', tournament.platform, icons.phone)}${field('Check-in Time', tournament.checkInTime, icons.clock)}${field('Start Time', tournament.startTime, icons.play)}${field('Registration Deadline', tournament.registrationDeadline ? date(tournament.registrationDeadline) : '', icons.calendar)}${field('Entry Fee', fee, icons.ticket)}</div><div class="prejoin-about"><h2>About Tournament</h2><p>${esc(tournament.description || 'Tournament information will be published by the organizer.')}</p><div><small>Organizer</small><strong>${esc(tournament.createdBy || 'WaveHub Official')}</strong><a href="messages.html?to=${encodeURIComponent(tournament.createdBy || 'admin')}">Contact Organizer</a></div></div><div class="prejoin-info"><h2>Tournament Info</h2>${field('Team Size', tournament.teamSize || tournament.format)}${field('Minimum Rank', tournament.minimumRank)}${field('Bracket Type', tournament.bracketType)}${field('Matches', tournament.matchesFormat)}${field('Who Can Join', tournament.eligibility)}${field('Communication', tournament.communication)}</div></div></section><section class="prejoin-register"><div><strong>Ready to Compete?</strong><span>Secure your spot and battle for glory.</span></div><div><small>Spots left</small><strong>${max - players} / ${max}</strong></div><div><small>Entry fee</small><strong>${esc(fee)}</strong></div><button id="tdRegisterNow" type="button">Register Now</button></section></section>`; byId('tdRegisterNow').onclick = () => { const session = (() => { try { return JSON.parse(localStorage.getItem(sessionKey) || 'null'); } catch { return null; } })(); if (!session?.user?.username) { location.href = 'auth.html?mode=login'; return; } const all = read(); const index = all.findIndex((item) => String(item.id) === String(id)); if (index < 0) return; const users = Array.isArray(all[index].registeredUsers) ? all[index].registeredUsers : []; if (!users.includes(session.user.username)) { all[index].registeredUsers = [...users, session.user.username]; all[index].players = Math.min(Number(all[index].maxPlayers) || 64, (Number(all[index].players) || 0) + 1); localStorage.setItem(key, JSON.stringify(all)); } location.reload(); }; }
  function render() { tournament = read().find((item) => String(item.id) === String(id)); if (!tournament) { byId('tournamentDetailPage').innerHTML = '<div class="td-not-found"><h1>Tournament not found</h1><p>This tournament may have been removed.</p><a href="tournaments.html">Back to tournaments</a></div>'; return; } document.title = `${tournament.name} Results - WaveHubX`; const image = tournament.coverData || covers[tournament.game] || covers['PUBG Mobile']; byId('resultsGameCover').style.backgroundImage = `linear-gradient(90deg,rgba(3,7,15,.05),rgba(3,7,15,.72)),url("${String(image).replaceAll('"', '%22')}")`; byId('tdName').textContent = tournament.name || 'Tournament'; const status = String(tournament.status || 'upcoming').toLowerCase(); byId('tdStatus').className = `results-status ${status}`; byId('tdStatus').innerHTML = `<i></i>${status === 'completed' ? 'Completed' : status === 'open' ? 'In Progress' : 'Upcoming'}`; byId('tdDate').innerHTML = `${icons.calendar}${date(tournament.startDate)}`; byId('tdRegion').innerHTML = `${icons.globe}${esc(tournament.region || 'Server TBA')}`; byId('tdFormat').innerHTML = `${icons.users}${esc(tournament.format || 'Format TBA')}`; byId('tdTeam').textContent = tournament.teamName || 'No team assigned'; const matches = Array.isArray(tournament.matches) ? tournament.matches : []; const shown = filter === 'all' ? matches : matches.filter((match) => kind(match) === filter); byId('matchList').innerHTML = shown.map(row).join(''); byId('matchEmpty').hidden = shown.length > 0; byId('matchHistoryDescription').textContent = matches.length ? 'All published matches are shown from newest to oldest.' : 'Published matches appear here in real time.'; }
  function renderPrizePanel() { const panel = document.querySelector('.prejoin-grid'); if (!panel) return; const prizes = Array.isArray(tournament.prizeDistribution) && tournament.prizeDistribution.length ? tournament.prizeDistribution : [{ place: '1st Place', amount: '500 GEL', rewards: ['Gaming Gear', '2-Night Hotel Stay'] }, { place: '2nd Place', amount: '300 GEL', rewards: ['Sponsor Gift'] }, { place: '3rd Place', amount: '200 GEL', rewards: [] }]; const rewards = Array.isArray(tournament.specialRewards) && tournament.specialRewards.length ? tournament.specialRewards : ['MVP Reward', 'Partner Gift', 'Travel / Hotel Voucher']; const prizeIcons = { trophy: '<svg viewBox="0 0 24 24"><path d="M7 3h10v5a5 5 0 0 1-10 0V3Z"/><path d="M7 5H3v2a4 4 0 0 0 4 4m10-6h4v2a4 4 0 0 1-4 4M12 13v4m-4 4h8m-6-4h4"/></svg>', coins: '<svg viewBox="0 0 24 24"><ellipse cx="12" cy="6" rx="6" ry="2.5"/><path d="M6 6v7c0 1.4 2.7 2.5 6 2.5s6-1.1 6-2.5V6M6 10c0 1.4 2.7 2.5 6 2.5s6-1.1 6-2.5M6 13v5c0 1.4 2.7 2.5 6 2.5s6-1.1 6-2.5v-5"/></svg>', gift: '<svg viewBox="0 0 24 24"><path d="M4 10h16v10H4V10Zm8 0v10M3 6h18v4H3V6Zm6-3c0 2 3 3 3 3s3-1 3-3c0-1.3-1.6-1.8-3 0-1.4-1.8-3-.7-3 0Z"/></svg>', star: '<svg viewBox="0 0 24 24"><path d="m12 3 2.8 5.7 6.2.9-4.5 4.4 1.1 6.2-5.6-3-5.6 3 1.1-6.2L3 9.6l6.2-.9L12 3Z"/></svg>', plane: '<svg viewBox="0 0 24 24"><path d="m3 11 18-8-7 18-3-7-8-3Z"/><path d="m11 14 4-4"/></svg>', info: '<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="8.5"/><path d="M12 10v5m0-8v.2"/></svg>' }; const rows = prizes.slice(0, 3).map((item, index) => { const place = item.place || `${index + 1}${index === 0 ? 'st' : index === 1 ? 'nd' : 'rd'} Place`; const itemRewards = Array.isArray(item.rewards) ? item.rewards : item.reward ? [item.reward] : []; return `<article class="prize-row place-${index + 1}"><span class="prize-place-icon">${prizeIcons.trophy}</span><strong>${esc(place)}</strong><span class="prize-amount">${prizeIcons.coins}${esc(item.amount || 'Not announced')}</span>${itemRewards.map((reward) => `<span class="prize-extra">${prizeIcons.gift}${esc(reward)}</span>`).join('')}</article>`; }).join(''); const specialIcons = [prizeIcons.star, prizeIcons.gift, prizeIcons.plane]; panel.innerHTML = `<aside class="prize-total"><small>Total Prize Value</small><strong>${esc(tournament.prize || '1,000 GEL')}</strong><b>+ Bonus Rewards</b><i></i><p>${esc(tournament.bonusRewards || 'The prize pool may include cash, gifts, experiences, and sponsor rewards.')}</p><span>${prizeIcons.gift}</span></aside><section class="prize-breakdown"><h2>Prize Breakdown</h2><div class="prize-rows">${rows}</div><h3>Special Rewards</h3><div class="special-rewards">${rewards.slice(0, 3).map((reward, index) => `<span>${specialIcons[index % specialIcons.length]}${esc(reward)}</span>`).join('')}</div><p class="prize-note">${prizeIcons.info}Rewards may include cash, products, vouchers, or experience-based prizes depending on the tournament.</p></section>`; }
  function renderRulesPanel() { const panel = document.querySelector('.prejoin-grid'); if (!panel) return; const rules = Array.isArray(tournament.rules) ? tournament.rules : []; panel.innerHTML = `<section class="rules-panel"><h2>Tournament Rules</h2>${rules.length ? rules.map((rule, index) => `<article class="rule-row"><b>${index + 1}</b><strong>${esc(rule.title || `Rule ${index + 1}`)}</strong><p>${esc(rule.description || rule)}</p></article>`).join('') : '<p class="rules-pending">Tournament rules have not been published by the organizer yet.</p>'}</section>`; }
  function renderTeamsPanel() { const panel = document.querySelector('.prejoin-grid'); if (!panel) return; const publishedTeams = Array.isArray(tournament.teams) ? tournament.teams : []; const demoTeams = [{ name:'4AM Esports', tag:'4AM', captain:'Zer0', coach:'Luna', verified:true, members:['Zer0','Paraboy','Suk','33Svan','Order'] }, { name:'Team Queso', tag:'TQ', captain:'QuesoGG', coach:'Storm', verified:true, members:['QuesoGG','Reaper','Mafiaboy','Synzx','Tico'] }, { name:'Nova X', tag:'NVX', captain:'NOVA', coach:'Aster', verified:true, members:['NOVA','Lixaa','Shy','Kray','Frost'] }, { name:'Blacklist International', tag:'BLCK', captain:'BLCK.J', coach:'Raze', verified:true, members:['BLCK.J','Raven','Kairo','Jeky','Hades'] }, { name:'Reborn', tag:'R8', captain:'Re8Captain', coach:'Viper', verified:false, members:['Re8Captain','Skyy','Wizz','Vexx','Player'] }, { name:'Wave Riders', tag:'WRD', captain:'Wave Leader', coach:'Pulse', verified:false, members:['Wave Leader','Ocean','Storm','Surge','Kai'] }]; const teams = publishedTeams.length ? publishedTeams : demoTeams; const maxTeams = Number(tournament.maxTeams) || Math.max(16, teams.length); const teamRow = (team, index) => { const members = (Array.isArray(team.members) ? team.members : []).slice(0, 5); const limit = Number(team.maxPlayers) || Number(tournament.teamSize) || 5; return `<article class="team-row" data-team-name="${esc(team.name)}" data-verified="${team.verified ? '1' : '0'}"><span class="team-number">${String(index + 1).padStart(2, '0')}</span><div class="team-identity"><div class="team-mark">${esc((team.tag || team.name || '?').slice(0, 3))}</div><div class="team-name"><strong>${esc(team.name || 'Team')}</strong><b>${esc(team.tag || '')}</b><small>Captain: ${esc(team.captain || 'Not announced')}${team.coach ? ` · Coach: ${esc(team.coach)}` : ''}</small></div></div><div class="team-members">${members.map((member) => `<span><i>${esc(String(member).slice(0, 1))}</i><small>${esc(member)}</small></span>`).join('')}</div><div class="team-size"><strong>${members.length} / ${limit}</strong><small>Players</small></div><b class="team-state ${team.verified ? 'verified' : 'pending'}">${team.verified ? 'Verified' : 'Pending'}</b><button class="team-view" type="button" data-team="${esc(team.name || 'Team')}">View Team</button></article>`; }; panel.innerHTML = `<section class="teams-panel"><header class="teams-toolbar"><h2>Registered Teams <strong>${teams.length}</strong><span>/ ${maxTeams} Teams</span></h2><div><label><span class="sr-only">Search teams</span><input id="teamSearch" type="search" placeholder="Search teams..."></label><select id="teamSort" aria-label="Team sorting"><option value="verified">Verified First</option><option value="name">Name A-Z</option></select></div></header><div class="team-table"><div class="team-table-head"><span>#</span><span>Team</span><span>Players</span><span>Squad Size</span><span>Status</span><span>Actions</span></div><div class="team-list">${teams.map(teamRow).join('')}</div></div></section>`; const filterTeams = () => { const query = byId('teamSearch')?.value.trim().toLowerCase() || ''; const sort = byId('teamSort')?.value; const rows = [...document.querySelectorAll('.team-row')]; rows.forEach((row) => { row.hidden = !row.textContent.toLowerCase().includes(query); }); rows.sort((a, b) => sort === 'name' ? a.dataset.teamName.localeCompare(b.dataset.teamName) : Number(b.dataset.verified) - Number(a.dataset.verified)).forEach((row) => row.parentElement.append(row)); }; byId('teamSearch')?.addEventListener('input', filterTeams); byId('teamSort')?.addEventListener('change', filterTeams); panel.querySelectorAll('.team-view').forEach((button) => button.addEventListener('click', () => { const name = button.dataset.team; const team = teams.find((item) => item.name === name); if (team?.detailsUrl) location.href = team.detailsUrl; else button.closest('.team-row')?.classList.toggle('expanded'); })); }
  const renderResults = render;
  render = function () { const current = read().find((item) => String(item.id) === String(id)); const session = (() => { try { return JSON.parse(localStorage.getItem(sessionKey) || 'null'); } catch { return null; } })(); if (current && !(Array.isArray(current.registeredUsers) && current.registeredUsers.includes(session?.user?.username))) { tournament = current; document.title = `${tournament.name} - WaveHubX`; renderPrejoin(); return; } renderResults(); };
  byId('matchFilters')?.addEventListener('click', (event) => { const button = event.target.closest('[data-match-filter]'); if (!button) return; filter = button.dataset.matchFilter; document.querySelectorAll('[data-match-filter]').forEach((item) => item.classList.toggle('active', item === button)); render(); });
  document.addEventListener('click', (event) => { const button = event.target.closest('.prejoin-details nav button'); if (!button) return; const tab = button.textContent.trim(); document.querySelectorAll('.prejoin-details nav button').forEach((item) => item.classList.toggle('active', item === button)); if (tab === 'General') renderPrejoin(); if (tab === 'Prize Pool') renderPrizePanel(); if (tab === 'Rules') renderRulesPanel(); if (tab === 'Teams') renderTeamsPanel(); });
  const toggle = byId('menuToggle'); const scrim = byId('scrim'); const sidebar = (open) => { document.body.classList.toggle('sidebar-open', open); if (scrim) scrim.hidden = !open; }; toggle?.addEventListener('click', () => sidebar(!document.body.classList.contains('sidebar-open'))); scrim?.addEventListener('click', () => sidebar(false)); window.addEventListener('storage', (event) => { if (event.key === key) render(); }); window.addEventListener('wavehub:tournament-results-updated', render); render();
}());
